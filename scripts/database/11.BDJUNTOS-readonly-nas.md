-- =============================================================================
-- BDJUNTOS / ConectaJuntos — SOLO LECTURA desde el módulo NAS
-- =============================================================================
--
-- Si el admin muestra "0 usuarios", verificar MSSQL_USER_SERVER en .env (ConectaJuntos)
-- y MSSQL_NAS_SERVER (DB_NAS). Pueden ser servidores SQL distintos.
--
-- Ejemplo .env:
--   MSSQL_NAS_SERVER=192.168.125.31
--   MSSQL_NAS_DATABASE=DB_NAS
--   MSSQL_USER_SERVER=Serversql
--   MSSQL_USERDATABASE=BDJUNTOS
--
-- Diagnóstico rápido en SQL (ejecutar en el servidor del .env):
--   SELECT @@SERVERNAME;
--   SELECT COUNT(DISTINCT ur.codUsuario)
--   FROM BDJUNTOS.INTRANET.tbl_ctrl_usuario_role ur
--   INNER JOIN BDJUNTOS.INTRANET.tbl_ctrl_usuarios_rol r ON r.iId = ur.iIdRole
--   WHERE ur.bEstado = 1
--     AND (r.vNombre LIKE N'%NAS%' OR ur.iIdRole BETWEEN 1072 AND 1080);
-- Debe ser > 0 para que aparezcan usuarios en /admin.
--
-- El backend NAS lee usuarios y roles NAS desde:
--   BDJUNTOS.INTRANET.tbl_mst_usuarios
--   BDJUNTOS.INTRANET.tbl_ctrl_usuario_role
--   BDJUNTOS.INTRANET.tbl_ctrl_usuarios_rol
--   BDJUNTOS.VIATICO.tbl_mst_usuario (vClave login)
--
-- La configuración del módulo (cuota, rol NAS local, categorías, privado) se
-- guarda únicamente en DB_NAS (NASTM_USUARIOS, NASTD_USUARIO_CATEGORIAS).
--
-- Los roles NAS (1072–1080) y sus asignaciones se gestionan en ConectaJuntos.
-- Este proyecto NO debe ejecutar INSERT/UPDATE/DELETE en BDJUNTOS.
--
-- Roles NAS esperados:
--   1072  00 UTI - Admin NAS
--   1073  00 UCI - NAS
--   1074  00 UAS - NAS
--   1075  00 UOP - NAS
--   1076  00 UTI - NAS
--   1077  00 UA - NAS
--   1078  00 UI - NAS
--   1079  00 UPPM- NAS
--   1080  00 URH- NAS
--
-- =============================================================================
